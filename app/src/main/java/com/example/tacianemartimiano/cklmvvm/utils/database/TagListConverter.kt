package com.example.tacianemartimiano.cklmvvm.utils.database

class TagListConverter {
/*
    @TypeConverter
    fun fromString(str: String): List<Tag> {
        var tags = listOf()

        val values = str.split("/")
        val labels = values[0]
        val colors = values[1]

        for (i in labels.length){

        }

        val tag = Tag()
        tag.label

        return tags
    }

    @TypeConverter
    fun tagToString(list: List<Tag>?): String {
        var str = String()

        if (list != null) {
            for (tag in list) {
                str += "${tag.label}+"
            }

            str += "/"

            for (tag in list)
                str += "${tag.color}+"
            }
        }
        return str
    }
*/
}
